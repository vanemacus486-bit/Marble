# 更新日志

本文件记录 Marble 每个版本的变化，会显示在「设置 → 关于」页面。

## 0.2.0 — AI 写网页 + 设置改版

- 核心范式转为「AI 写、你看」：中间改为只读 HTML 渲染（沙箱 iframe），移除编辑器
- AI 改为产出完整 HTML 网页，边写边在中间实时渲染，并写回当前 `.html` 笔记
- 设置页改版为「左分类 + 右内容」：AI / 写作偏好 / 笔记库 / 外观 / 关于
- 新增「写作偏好」：自定义默认文档要求，统一所有 AI 笔记的排版与语气
- 新增「笔记库」随时切换文件夹
- 新增「外观」深色 / 浅色界面主题
- 移除 Vditor 依赖，安装包更小

## 0.1.1 — 修复模型名

- 修正默认模型 id 为小写 `agnes-2.0-flash`（Agnes 模型名区分大小写，旧的 `Agnes-2.0-Flash` 会报 503 model_not_found）
- 旧配置自动纠正：检测到旧的大小写模型名时自动改为正确值
- 设置 → AI 新增「拉取可用模型」按钮：列出你账号下所有可用模型，点选即可填入

## 0.1.0 — 首个可用版本

- 三栏布局：左侧文件树 / 中间 Vditor 编辑器 / 右侧 AI 对话
- 本地笔记库（vault）：选择一个文件夹即可，直接读写真实 `.md` 文件
- 文件树：新建笔记 / 新建文件夹 / 重命名 / 删除
- 编辑器：基于 Vditor，自动保存，字数统计
- AI 对话：接入 Agnes 免费 API（OpenAI 兼容），流式输出，自动带上当前笔记作为上下文
- 一键把 AI 回答「插入到当前笔记」或「新建为笔记」
- 设置：AI 配置（API Key / 接口地址 / 模型）+ 关于（构建时间、版本、更新日志）
